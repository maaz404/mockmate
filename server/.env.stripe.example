# Stripe Configuration Guide
# ==============================
# Follow the steps in STRIPE_SETUP_GUIDE.md to get these values

# REQUIRED: Get these from Stripe Dashboard (https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# REQUIRED: Create a Premium product in Stripe Dashboard, then get the price ID
# 1. Go to Products → Add Product
# 2. Name: "MockMate Premium"
# 3. Price: $29.99 (or your chosen amount)
# 4. Billing: Recurring (monthly)
# 5. Copy the price ID that starts with price_
STRIPE_PREMIUM_PRICE_ID=price_your_price_id_here

# REQUIRED: Set up webhook endpoint and get signing secret
# 1. Go to Developers → Webhooks → Add endpoint
# 2. URL: https://yourdomain.com/api/payments/webhook (or http://localhost:5000/api/payments/webhook for testing)
# 3. Events to listen for:
#    - checkout.session.completed
#    - customer.subscription.updated
#    - customer.subscription.deleted
#    - invoice.payment_succeeded
#    - invoice.payment_failed
# 4. Copy the webhook signing secret that starts with whsec_
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# REQUIRED: Frontend URL for redirects after checkout
# Production: https://yourdomain.com
# Development: http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Testing with Stripe CLI (Optional - for local webhook testing)
# ================================================================
# 1. Install Stripe CLI: https://stripe.com/docs/stripe-cli
# 2. Run: stripe login
# 3. Run: stripe listen --forward-to localhost:5000/api/payments/webhook
# 4. Copy the webhook signing secret from the terminal output
# 5. Update STRIPE_WEBHOOK_SECRET above with the CLI secret

# Test Cards
# ================================================================
# Success: 4242 4242 4242 4242
# Decline: 4000 0000 0000 0002
# Requires Auth: 4000 0027 6000 3184
# Use any future expiration date and any CVC
